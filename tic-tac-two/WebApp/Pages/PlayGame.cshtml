@page
@inject IJSRuntime
@using Microsoft.JSInterop
@model WebApp.Pages.PlayGame

<div class="container-fluid p-5 bg-primary text-white text-center">
    <h1>PLAY!</h1>
</div>

<div>
    <div class="board">
        @for (int i = 0; i < @Model.GameState.Config.Width; i++)
        {
            @for (int j = 0; j < @Model.GameState.Config.Height; j++)
            {
                
                
                
                <!--- <div class="square"
                     @onclick="() => SquareClicked(i, j)">
                    <h5 class="char">@Model.GameState.Board[i, j]</h5>
                </div> --->
            }
        }
    </div>
</div>

